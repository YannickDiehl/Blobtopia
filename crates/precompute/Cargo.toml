[package]
name = "blobtopia-precompute"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "blobtopia-precompute"
path = "src/main.rs"

[dependencies]
simulation-core = { path = "../simulation-core" }
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = "1.0"
