node_modules/
dist/
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Lock files (using npm only)
yarn.lock

# Python cache
__pycache__/
*.pyc

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

# Rust build
target/

# Generated/large data files
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db.bak
data/blobtopia_state.json

# Generated timeline tick blocks (~2.3 GB; regenerate via `npm run export`
# from data/blobtopia_timeline.db — see data/README.md). Everything else
# under public/data/timeline/ (meta, blobs-static, buildings, stats, tweets,
# newspapers, ...) is small and tracked so a fresh clone can run the app.
public/data/timeline/ticks/

# 3D models (binary, tracked via Vercel upload)
public/models/
src/assets/models/
*.glb

# Temporary LLM batch/result files (intermediate generation artifacts)
data/_batch_*.json
data/_result_*.json
data/_tweet_batch_*.json
data/_tweet_result_*.json
data/_sample_*.json

# Deployment
.vercel
.now

# Per-blob histories (not loaded by frontend, ~500 MB - cleanup 2026-05-12)
public/data/timeline/blobs/

# Backup files (any *.bak* under data/)
data/*.bak*
